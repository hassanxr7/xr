# Local development only. Copy to .env.local and adjust.
# Points the dev server's /api/* rewrite at the locally running API so the
# browser sees everything as same-origin (cookies/CSRF/SSE all just work).
API_PROXY_ORIGIN=http://localhost:3001

# Optional rebrand.
# NEXT_PUBLIC_BRAND_NAME=SMSBridge
